import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  button: {
    padding: 15,
    paddingLeft: 40,
    paddingRight: 40,
    alignItems: 'center',
    borderRadius: 5
  },
  smallButton: {
    width: 90,
    padding: 10,
    alignItems: 'center',
    borderRadius: 5
  }
});

export default style;